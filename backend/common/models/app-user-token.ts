import { Model } from '@mean-expert/model';
/**
 * @module AppUserToken
 * @description
 * Write a useful AppUserToken Model description.
 * Register hooks and remote methods within the
 * Model Decorator
 **/
@Model({
  hooks: { },
  remotes: { }
})

class AppUserToken {
  constructor(public model: any) {}
}

module.exports = AppUserToken;
